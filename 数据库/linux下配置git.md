```js
下载
yum install git
配置用户名和电子邮箱
git config --global user.name "runoob"
git config --global user.email test@runoob.com

查看已经配置的信息
git config --list

为GitHub账号添加SSH Key
ssh-keygen -t rsa -C "1960050004@qq.com"
cat ~/.ssh/id_rsa.pub
复制到github的ssh key上面 
```

